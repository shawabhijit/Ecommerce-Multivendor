import { createTheme } from "@mui/material";

export const customTheme = createTheme({
    palette: {
        mode: 'light',
        primary:{
            main: '#09c5a9',
        },
        secondary:{
            main: '#f50057',
        },
        
    }
});